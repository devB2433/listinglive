# 复制为 .env 后修改，勿提交 .env
# 数据库（PostgreSQL）- 本仓库 docker-compose 使用 5433 端口
DATABASE_URL=postgresql+asyncpg://listinglive:listinglive@localhost:5433/listinglive

# Redis - 本仓库 docker-compose 使用 6380 端口
REDIS_URL=redis://127.0.0.1:6380/0

# 容器时区；本地 restart-dev.ps1 会自动按宿主机时区覆盖，手动 docker compose 时可自行指定
CONTAINER_TIMEZONE=UTC

# 安全（生产必须修改）
SECRET_KEY=your-secret-key-change-in-production
# 登录态（可按需要调整）
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# 存储（本地目录）
STORAGE_TYPE=local
STORAGE_LOCAL_ROOT=./data/storage

# AI provider 配置文件路径
# 正常情况下无需修改，直接维护 config/ai_provider.toml 即可
AI_PROVIDER_CONFIG_PATH=config/ai_provider.toml

SCENE_TEMPLATE_CONFIG_PATH=config/scene_templates.json
TRANSITION_EFFECT_CONFIG_PATH=config/transition_effects.json

# 视频任务运行时
VIDEO_POLL_INTERVAL_SECONDS=5
VIDEO_MAX_POLL_SECONDS=300
VIDEO_FPS=24
VIDEO_GENERATE_TIMEOUT_SECONDS=600
VIDEO_MERGE_TIMEOUT_SECONDS=900
VIDEO_TASK_STALE_SECONDS=1800
VIDEO_EXPIRED_CLEANUP_BATCH_SIZE=100
VIDEO_PROVIDER_MAX_RETRIES=2
VIDEO_PROVIDER_RETRY_BACKOFF_SECONDS=5
# 第三方并发上限如果是 10，建议本地队列门默认保持 8，给重试和抖动留余量
VIDEO_PROVIDER_CONCURRENCY_LIMIT=8
VIDEO_PROVIDER_CONCURRENCY_WAIT_SECONDS=60
VIDEO_PROVIDER_QUEUE_HEARTBEAT_SECONDS=15
# 仅本地压测使用；例如设为 120 可模拟单任务处理 120 秒
LOCAL_VIDEO_PROVIDER_DELAY_SECONDS=0

# Stripe
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CURRENCY=cad
STRIPE_CHECKOUT_SUCCESS_URL=http://127.0.0.1:3001/billing/success?session_id={CHECKOUT_SESSION_ID}
STRIPE_CHECKOUT_CANCEL_URL=http://127.0.0.1:3001/billing/cancel
STRIPE_BILLING_PORTAL_RETURN_URL=http://127.0.0.1:3001/billing

# 邮件发送（开发环境可留空；生产建议使用 SMTP）
MAIL_PROVIDER=
MAIL_FROM=hello@listinglive.ca
MAIL_FROM_NAME=ListingLive
MAIL_REPLY_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_TIMEOUT_SECONDS=20

# 开发/测试（DEBUG=true 时 500 会返回具体错误信息）
DEBUG=true
ENABLE_TEST_ACCOUNT=true
